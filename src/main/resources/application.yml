server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/dev/h2/friendcom
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        format_sql : true